@model AUTistima.Models.ScreeningRequest
@using AUTistima.Models.Enums
@{
    ViewData["Title"] = $"Avaliar Triagem {Model.CodigoIdentificador}";
}

<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-lg-10">
            <!-- Breadcrumb -->
            <nav aria-label="breadcrumb" class="mb-4">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a asp-action="Index">Triagem Educacional</a></li>
                    <li class="breadcrumb-item"><a asp-action="Details" asp-route-id="@Model.Id">@Model.CodigoIdentificador</a></li>
                    <li class="breadcrumb-item active">Avaliar</li>
                </ol>
            </nav>

            <!-- Cabeçalho -->
            <div class="text-center mb-4">
                <h1 class="display-6 fw-bold">
                    <i class="bi bi-clipboard2-check text-success me-2"></i>
                    Avaliação da Triagem
                </h1>
                <p class="lead text-secondary">
                    Código: <code>@Model.CodigoIdentificador</code>
                </p>
            </div>

            <div class="row">
                <!-- Resumo do Caso (Sidebar esquerda) -->
                <div class="col-lg-4 mb-4">
                    <div class="card shadow-sm border-0 sticky-top" style="top: 20px;">
                        <div class="card-header bg-warning bg-opacity-25 py-3">
                            <h6 class="mb-0">
                                <i class="bi bi-file-text me-2"></i>
                                Resumo do Caso
                            </h6>
                        </div>
                        <div class="card-body">
                            <div class="mb-3">
                                <label class="form-label text-muted small mb-0">Aluno</label>
                                <p class="fw-semibold mb-2">@Model.NomeAluno (@Model.IdadeAluno anos)</p>
                            </div>
                            <div class="mb-3">
                                <label class="form-label text-muted small mb-0">Série/Ano</label>
                                <p class="fw-semibold mb-2">@Model.SerieAno</p>
                            </div>
                            @if (Model.Escola != null)
                            {
                                <div class="mb-3">
                                    <label class="form-label text-muted small mb-0">Escola</label>
                                    <p class="fw-semibold mb-2">@Model.Escola.Nome</p>
                                </div>
                            }
                            <hr />
                            <div class="mb-3">
                                <label class="form-label text-muted small mb-1">Sinais Observados</label>
                                <p class="small mb-0" style="white-space: pre-wrap;">@Model.SinaisObservados</p>
                            </div>
                            @if (!string.IsNullOrEmpty(Model.ContextoObservacao))
                            {
                                <hr />
                                <div>
                                    <label class="form-label text-muted small mb-1">Contexto</label>
                                    <p class="small mb-0" style="white-space: pre-wrap;">@Model.ContextoObservacao</p>
                                </div>
                            }
                        </div>
                    </div>
                </div>

                <!-- Formulário de Avaliação -->
                <div class="col-lg-8">
                    <div class="card shadow-sm border-0">
                        <div class="card-header bg-success bg-opacity-10 py-3">
                            <h5 class="mb-0">
                                <i class="bi bi-pencil-square me-2"></i>
                                Parecer Profissional
                            </h5>
                        </div>
                        <div class="card-body p-4">
                            <form asp-action="Avaliar" method="post">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="id" value="@Model.Id" />

                                <!-- Parecer -->
                                <div class="mb-4">
                                    <label for="parecerProfissional" class="form-label fw-semibold">
                                        <i class="bi bi-chat-left-text me-1"></i>
                                        Parecer da Avaliação <span class="text-danger">*</span>
                                    </label>
                                    <textarea name="parecerProfissional" id="parecerProfissional" 
                                              class="form-control" rows="6" required
                                              placeholder="Descreva sua análise profissional sobre os sinais observados..."></textarea>
                                    <div class="form-text">
                                        Sua análise técnica sobre os sinais descritos e comportamentos observados.
                                    </div>
                                </div>

                                <!-- Recomendações -->
                                <div class="mb-4">
                                    <label for="recomendacoes" class="form-label fw-semibold">
                                        <i class="bi bi-list-check me-1"></i>
                                        Recomendações
                                    </label>
                                    <textarea name="recomendacoes" id="recomendacoes" 
                                              class="form-control" rows="4"
                                              placeholder="Orientações para a família e escola..."></textarea>
                                    <div class="form-text">
                                        Sugestões de intervenções, adaptações ou acompanhamentos.
                                    </div>
                                </div>

                                <!-- Encaminhamento -->
                                <div class="mb-4">
                                    <label for="encaminhamento" class="form-label fw-semibold">
                                        <i class="bi bi-arrow-right-circle me-1"></i>
                                        Encaminhamento
                                    </label>
                                    <textarea name="encaminhamento" id="encaminhamento" 
                                              class="form-control" rows="3"
                                              placeholder="Especialidades ou serviços recomendados..."></textarea>
                                    <div class="form-text">
                                        Indique especialistas ou serviços para avaliação mais aprofundada.
                                    </div>
                                </div>

                                <!-- Status -->
                                <div class="mb-4">
                                    <label for="novoStatus" class="form-label fw-semibold">
                                        <i class="bi bi-flag me-1"></i>
                                        Novo Status <span class="text-danger">*</span>
                                    </label>
                                    <select name="novoStatus" id="novoStatus" class="form-select" required>
                                        <option value="">Selecione o status...</option>
                                        <option value="EmAvaliacao">🔵 Manter em Avaliação (pendente de mais informações)</option>
                                        <option value="Encaminhado">🟢 Encaminhado (necessita avaliação especializada)</option>
                                        <option value="Concluido">⚪ Concluído (sem indicativos significativos)</option>
                                    </select>
                                </div>

                                <!-- Aviso -->
                                <div class="alert alert-warning border-0 mb-4">
                                    <div class="d-flex">
                                        <div class="me-3">
                                            <i class="bi bi-exclamation-triangle fs-4"></i>
                                        </div>
                                        <div>
                                            <strong>Importante:</strong> Esta avaliação é um indicativo para encaminhamento, 
                                            não um diagnóstico. O diagnóstico de TEA deve ser realizado por equipe 
                                            multidisciplinar especializada.
                                        </div>
                                    </div>
                                </div>

                                <div class="d-flex gap-3">
                                    <button type="submit" class="btn btn-success btn-lg flex-grow-1">
                                        <i class="bi bi-check-lg me-2"></i>
                                        Registrar Avaliação
                                    </button>
                                    <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-outline-secondary btn-lg">
                                        <i class="bi bi-arrow-left me-1"></i>
                                        Cancelar
                                    </a>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
